{
  "name": "postman-sdk",
  "version": "1.0.0",
  "description": "Barebones SDK for the Postman API",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "src/index.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage --coverageReporters=json-summary --coverageReporters=text --coverageReporters=lcov",
    "test:badges": "npm run test:coverage",
    "test:getSpecs": "node scripts/test-getSpecs.js",
    "test:createSpec": "node scripts/test-createSpec.js"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "@jest/test-sequencer": "^30.2.0",
    "jest": "^29.7.0",
    "js-yaml": "^4.1.0"
  }
}
